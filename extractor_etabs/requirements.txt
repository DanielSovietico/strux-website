# Interfaz de escritorio.
PySide6>=6.5

# Exportación e importación de .xlsx (opcional: sin esto se usa CSV).
openpyxl>=3.1

# Enlace con la OAPI de ETABS. Solo Windows; se instala aparte:
#   pip install comtypes
# Alternativa:
#   pip install pythonnet
comtypes>=1.2 ; sys_platform == "win32"
